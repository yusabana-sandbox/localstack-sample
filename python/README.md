Localstack samples for python
=========

## Requirements

First of all, You should run LocalStack.

```
$ pip install -r requirements.txt
```


## DynamoDB samples

```
$ python -m unittest tests.test_dynamodb
```
